</div>
</div>
<div class="form-group"  style="font-size: 13px">
    <label class="col-sm-2 control-label" for="input-price-zak">{{ suppler2_data }}</label>
    <div class="col-sm-10" style="margin-bottom:10px;">
        <table class="table table-striped" style="font-size: 13px">
            <thead>
            <tr>
                <th><label>{{ text_suppler_name }}</label></th>
                <th><label>{{ text_price }}</label></th>
                <th><label>{{ text_quantity }}</label></th>
                <th><label>{{ text_date }}</label></th>
                <th><label>{{ text_source }}</label></th>
            </tr>
            </thead>
            <tbody>
            {% if rows %}
            {% for row in rows %}
                <tr>
                    <td>
                        <span>{{ row.suppler_name }}</span>
                    </td>
                    <td>
                        <span>{{ row.price }}</span>
                    </td>
                    <td>
                        <span>{{ row.quantity }}</span>
                    </td>
                    <td>
                        <span>{{ row.date }}</span>
                    </td>
                    <td>
                        <span>{{ row.source }}</span>
                    </td>
                </tr>
            {% endfor %}
            {% else %}
                {{ error_not_found }}
            {% endif %}
            </tbody>
        </table>
